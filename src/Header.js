import React from 'react';

const Header = () => (
    <header>
        <h1>Hello and welcome to my site!</h1>
    </header>
);

// we need to export Header, otherwise
// other files have no idea what Header
export default Header;